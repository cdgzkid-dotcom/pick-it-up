# Revisión Estática: Integración de Telegram (Pick It Up)

| Archivo:Línea | Problema | Severidad | Fix en 1 línea |
|---|---|---|---|
| `lib/telegram.ts:19-36` | `sendTelegramMessage` no valida ni divide mensajes que superan el límite de 4096 caracteres de Telegram (slates con muchos picks/análisis fallan con HTTP 400). | ALTA | Implementar división automática en fragmentos (<4096 caracteres) por saltos de línea antes de enviar. |
| `lib/telegram.ts:56-61` | Al fallar el parseo de Markdown con HTTP 400 (`can't parse entities`), la función aborta de inmediato sin reintentar enviando texto plano (`parse_mode: null`). | ALTA | Reintentar el envío una vez con `parse_mode: null` (o Markdown sanitizado) si el status es 400 por parse error. |
| `lib/telegram.ts:299-304` | `renderHealthIndicator` incluye nombres de checks con guiones bajos sin escapar (`env_vars`, `supabase_read`, `espn_predictor_*`), rompiendo el parseo de cursiva en Markdown. | ALTA | Escapar guiones bajos (`_` a `\_`) o reemplazar por espacios en `errorNames` y `warningNames` antes de interpolar. |
| `lib/telegram.ts:641-643` | `formatPickDigestMessage` concatena directamente `failures` (`market_sources_count_below_2`, etc.) con múltiples guiones bajos, provocando rechazo HTTP 400 de Telegram. | ALTA | Escapar guiones bajos o envolver los nombres de fallas en backticks/texto plano antes de agregarlos al mensaje. |
| `app/api/cron/analyze/route.ts:714-725` | Si `sendTelegramMessage` falla, `telegram_notified_at` queda `null` pero los picks persisten como `pending`, quedando bloqueados en `alreadyDone` y nunca siendo notificados en crons posteriores. | ALTA | Incluir un bloque al inicio del cron para reintentar la notificación de picks en `pending` con `telegram_notified_at IS NULL`. |
| `app/api/telegram/webhook/route.ts:417-424` | El webhook no valida el encabezado `X-Telegram-Bot-Api-Secret-Token`, permitiendo que terceros envíen updates falsos y ejecuten llamadas costosas a Claude Vision. | ALTA | Validar `req.headers.get('x-telegram-bot-api-secret-token') === process.env.TELEGRAM_WEBHOOK_SECRET` al inicio de `POST`. |
| `app/api/telegram/webhook/route.ts:437, 333` | El webhook procesa fotos y callbacks de cualquier `chatId` sin verificar que coincida con `process.env.TELEGRAM_CHAT_ID`, permitiendo uso no autorizado del bot. | ALTA | Rechazar updates cuyo `chatId !== Number(process.env.TELEGRAM_CHAT_ID)`. |
| `app/api/telegram/webhook/route.ts:164-166, 328-329` | `formatPreview` usa `parse_mode: 'Markdown'` interpolando texto OCR (`leg.selection`, `leg.teams`, `match.pick.pick`) sin escapar; si falla, `tgEdit` es rechazado y el usuario queda congelado en "Analizando...". | ALTA | Escapar caracteres especiales de Markdown en textos de OCR/matching o enviar la vista previa en texto plano. |
| `app/api/check-results/route.ts:280-320` | La consulta de catch-up acumula todas las apuestas no notificadas en un solo mensaje; un backlog grande (>25 apuestas) excede 4096 caracteres y bloquea indefinidamente las notificaciones. | ALTA | Enviar las resoluciones en lotes de máximo 10-15 apuestas por mensaje de Telegram. |
| `lib/telegram.ts:361-366, 420-421, 517-526` | Campos dinámicos (`p.pick`, `p.away_team`, `p.home_team`, `p.best_odds_source`, `s.pick`) se concatenan sin sanitizar caracteres de Markdown (`_`, `*`, `[`). | MEDIA | Crear y aplicar un helper `escapeMarkdown(str)` en todas las variables dinámicas antes de agregarlas a `lines`. |
| `lib/telegram.ts:84-86` | El backoff ante HTTP 429 trunca la espera a `TELEGRAM_MAX_RETRY_MS` (1s), ignorando `retry_after` (5-60s) de Telegram y agotando los 3 reintentos inútilmente. | MEDIA | Respetar `retry_after` hasta un límite seguro (ej. 5s) o abortar de inmediato con error estructurado de rate-limit. |
| `app/api/cron/calibrate/route.ts:183, 191` | `w.factor_name` (ej. `pitcher_era`, `bullpen_whip_l10`) contiene guiones bajos y se inserta sin escapar en el reporte semanal, provocando errores de parseo en Telegram. | MEDIA | Envolver `w.factor_name` en backticks (código inline) o escapar guiones bajos antes de generar las líneas. |
| `app/api/cron/heartbeat/route.ts:41-44` | Tokens con guion bajo (`no_edge`, `no_odds`, `audit_filtered`) se agregan a `breakdownParts` sin escapar, abriendo etiquetas de cursiva huérfanas en Markdown. | MEDIA | Reemplazar guiones bajos por espacios o nombres legibles (ej. `'no edge'`) en el desglose del heartbeat. |
| `app/api/cron/analyze/route.ts:422-423` | El resultado de `sendTelegramMessage` para picks retirados (`formatSupersededOnlyMessage`) se ignora; si falla, las apuestas ya quedaron marcadas en BD y nunca se renotifican. | MEDIA | Verificar `sent.ok` y reintentar o registrar el fallo para renotificar picks retirados en la siguiente ejecución. |
| `app/api/telegram/webhook/route.ts:51, 418-436` | `update_id` no se almacena ni se deduplica; reintentos de red de Telegram procesan la misma foto múltiples veces, duplicando llamadas a Claude Vision y sesiones. | MEDIA | Registrar `update_id`s recientes en Redis/Supabase con TTL de 24h e ignorar updates duplicados. |
| `app/api/telegram/webhook/route.ts:356-374` | La lectura y borrado de `telegram_sessions` en `handleCallback` no son atómicos; clics dobles rápidos en "Confirmar" pueden procesar dos veces la misma apuesta. | MEDIA | Utilizar una operación atómica de eliminación con retorno (`DELETE ... RETURNING payload` o RPC) para consumir la sesión una sola vez. |
| `app/api/telegram/webhook/route.ts:375-380` | La llamada `fetch` interna a `/api/bets/from-image/confirm` dentro de `waitUntil` carece de `try/catch`; un fallo de red genera una promesa rechazada no capturada y deja la UI sin feedback. | MEDIA | Envolver el `fetch` en un bloque `try/catch` y editar el mensaje en Telegram notificando el error de conexión. |
| `app/api/cron/analyze/route.ts:339` | El envío de alerta de playoffs analizados sin edge ignora el valor de retorno de `sendTelegramMessage` tras haber insertado los marcadores en la base de datos. | BAJA | Registrar el resultado del envío en los logs y agregar manejo de error si el envío falla. |
| `app/api/health/full/route.ts:138-142` | La alerta crítica de salud del sistema (`alertLevel === 'red'`) se dispara con `void sendTelegramMessage(...)` sin `await` ni manejo de error. | BAJA | Agregar `await` a `sendTelegramMessage` y registrar advertencia si `sent.ok === false`. |
| `app/api/telegram/webhook/route.ts:439-446` | Cualquier mensaje de texto o comando (`/start`, `/help`, etc.) responde con el texto genérico de captura de pantalla sin enrutamiento ni ayuda contextual. | BAJA | Agregar un switch para comandos básicos (`/start`, `/help`, `/status`) con respuestas específicas. |
